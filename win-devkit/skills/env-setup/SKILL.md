---
name: env-setup
description: Project environment setup. Run scripts/setup.ps1 first.
---

# Environment setup

Required command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\setup.ps1
```

Always run this before diagnosing other project issues.
